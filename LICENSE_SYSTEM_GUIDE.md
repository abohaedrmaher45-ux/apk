# 🔐 دليل المطور لنظام الترخيص

## 📖 نظرة عامة
تم إصلاح نظام الترخيص بنجاح! التطبيق الآن يحتوي على:

### ✨ المميزات الجديدة
1. **فترة تجريبية 24 ساعة كاملة** - يحصل المستخدم على يوم كامل للتجربة
2. **نظام تفعيل آمن** - يتطلب ID مشفر مرتبط بالجهاز
3. **واجهة ترحيبية** - تظهر للمستخدم عند أول تشغيل
4. **شاشة تفعيل محسّنة** - مع حقلين: اسم المستخدم وكود التفعيل
5. **حماية من التلاعب** - استخدام NTP للوقت ومنع التلاعب بالتاريخ

---

## 🔑 كيفية الحصول على كود التفعيل

### الطريقة 1: استخدام سكريبت مساعد (للمطورين)

قم بإنشاء ملف Python باسم `generate_activation_code.py`:

```python
import hashlib

# المفتاح السري (يجب أن يطابق المفتاح في ValidationService)
SECRET_KEY = "MySuperSecretKey2024"

def generate_activation_code(device_id):
    """
    توليد كود التفعيل بناءً على معرف الجهاز
    """
    combined = device_id + SECRET_KEY
    hash_object = hashlib.sha256(combined.encode())
    full_hash = hash_object.hexdigest()
    
    # نأخذ أول 16 حرف ونجعلها بأحرف كبيرة
    activation_code = full_hash[:16].upper()
    return activation_code

# مثال على الاستخدام
if __name__ == "__main__":
    print("=" * 50)
    print("🔑 مولّد أكواد التفعيل")
    print("=" * 50)
    
    # طلب معرف الجهاز من المستخدم
    device_id = input("\n📱 أدخل معرف الجهاز (Device ID): ").strip()
    
    if device_id:
        activation_code = generate_activation_code(device_id)
        print(f"\n✅ كود التفعيل الخاص بك هو:")
        print(f"🔐 {activation_code}")
        print(f"\nيمكنك الآن استخدام هذا الكود لتفعيل التطبيق على الجهاز: {device_id}")
    else:
        print("❌ خطأ: لم يتم إدخال معرف الجهاز!")
```

**طريقة التشغيل:**
```bash
python3 generate_activation_code.py
```

---

### الطريقة 2: إنشاء تطبيق مساعد لجمع Device ID

قم بإنشاء تطبيق Flutter بسيط يقوم بعرض Device ID للمستخدم:

```dart
import 'package:flutter/material.dart';
import 'package:device_info_plus/device_info_plus.dart';
import 'dart:io';

class DeviceInfoScreen extends StatefulWidget {
  @override
  _DeviceInfoScreenState createState() => _DeviceInfoScreenState();
}

class _DeviceInfoScreenState extends State<DeviceInfoScreen> {
  String deviceId = 'جارٍ التحميل...';

  @override
  void initState() {
    super.initState();
    _getDeviceId();
  }

  Future<void> _getDeviceId() async {
    try {
      final DeviceInfoPlugin deviceInfo = DeviceInfoPlugin();
      
      if (Platform.isAndroid) {
        AndroidDeviceInfo androidInfo = await deviceInfo.androidInfo;
        setState(() {
          deviceId = androidInfo.id;
        });
      }
    } catch (e) {
      setState(() {
        deviceId = 'خطأ في الحصول على المعرف';
      });
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('معرف الجهاز')),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text('معرف الجهاز الخاص بك:', style: TextStyle(fontSize: 20)),
            SizedBox(height: 16),
            SelectableText(
              deviceId,
              style: TextStyle(
                fontSize: 16,
                fontFamily: 'monospace',
                color: Colors.blue,
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```

---

### الطريقة 3: توليد كود مباشرة باستخدام سطر أوامر

```bash
# استبدل DEVICE_ID_HERE بمعرف الجهاز الفعلي
echo -n "DEVICE_ID_HEREMySuperSecretKey2024" | sha256sum | cut -c1-16 | tr '[:lower:]' '[:upper:]'
```

---

## 📋 خطوات التفعيل للمستخدم النهائي

1. **التثبيت الأول:**
   - عند فتح التطبيق لأول مرة، ستظهر رسالة ترحيب
   - سيحصل المستخدم على فترة تجريبية مدتها 24 ساعة كاملة

2. **خلال الفترة التجريبية:**
   - يمكن استخدام جميع مميزات التطبيق
   - سيظهر العداد التنازلي للساعات المتبقية في الشريط العلوي

3. **بعد انتهاء الفترة التجريبية:**
   - ستظهر شاشة التفعيل تلقائياً
   - يطلب من المستخدم إدخال:
     - **اسم المستخدم** (أي اسم)
     - **كود التفعيل** (ID مكون من 16 حرف)

4. **التفعيل:**
   - بمجرد إدخال الكود الصحيح، يتم تفعيل التطبيق للأبد
   - لن يحتاج المستخدم للتفعيل مرة أخرى

---

## 🔒 الأمان والحماية

### المميزات الأمنية المطبقة:

1. **مزامنة الوقت عبر الإنترنت (NTP):**
   - يمنع تلاعب المستخدم بتاريخ الجهاز
   - يحفظ آخر وقت معروف صحيح

2. **ملف خفي في التخزين الخارجي:**
   - يحفظ تاريخ أول تثبيت
   - يمنع إعادة الفترة التجريبية عند إعادة التثبيت

3. **تشفير البيانات المحلية:**
   - استخدام `flutter_secure_storage`
   - حماية بيانات المستخدم والترخيص

4. **كود التفعيل مرتبط بالجهاز:**
   - كل جهاز له كود تفعيل فريد
   - لا يمكن استخدام نفس الكود على أجهزة مختلفة

---

## 🧪 الاختبار

### للاختبار على الويب:
رابط المعاينة: https://5060-iuaahmlk0myv79ql8u1oc-cc2fbc16.sandbox.novita.ai

⚠️ **ملاحظة:** بعض المميزات لن تعمل بشكل كامل على الويب:
- التحقق من Device ID (خاص بالأندرويد)
- الملف الخفي في التخزين الخارجي
- الوقت الفعلي قد يكون تقريبياً

### للاختبار على الأندرويد:
1. قم ببناء APK: `flutter build apk --release`
2. ثبّت التطبيق على جهاز حقيقي
3. اختبر جميع السيناريوهات

---

## 🐛 إعادة تعيين الفترة التجريبية (للتطوير فقط)

في `license_service.dart`، توجد دالة `resetTrial()` للمطورين:

```dart
// احذف هذه الدالة في الإصدار النهائي!
await _licenseService.resetTrial();
```

⚠️ **مهم:** احذف هذه الدالة قبل إصدار التطبيق للمستخدمين النهائيين!

---

## 📊 ملخص التعديلات

### الملفات المعدّلة:
1. ✅ `pubspec.yaml` - إضافة المكتبات المطلوبة
2. ✅ `lib/license_service.dart` - نظام الترخيص الكامل (24 ساعة)
3. ✅ `lib/main.dart` - شاشة الفحص الأولي والترحيب
4. ✅ `lib/screens/activation_screen.dart` - شاشة التفعيل مع حقلين
5. ✅ `lib/services/time_security_service.dart` - موجود مسبقاً
6. ✅ `lib/services/validation_service.dart` - موجود مسبقاً
7. ✅ `lib/services/device_service.dart` - موجود مسبقاً

---

## 🚀 الإصدار النهائي

قبل إصدار التطبيق:

1. ✅ احذف دالة `resetTrial()` من `license_service.dart`
2. ✅ قم بتشفير `SECRET_KEY` في `validation_service.dart`
3. ✅ قم بتفعيل ProGuard للأندرويد
4. ✅ اختبر على أجهزة متعددة
5. ✅ راجع أذونات الأندرويد في `AndroidManifest.xml`

---

## 💬 الدعم

في حال وجود أي مشكلة:
- تأكد من وجود اتصال بالإنترنت (لمزامنة الوقت)
- تأكد من أذونات التخزين
- راجع سجلات الأخطاء في Logcat

---

**تم التطوير بواسطة:** Gen AI Assistant
**التاريخ:** 2025
**الإصدار:** 1.0.0
