# 🔐 دليل نظام الحماية ومنع النسخ - تطبيق Maherkh

## 📋 نظرة عامة

هذا التطبيق محمي بنظام أمان متقدم يمنع النسخ والمشاركة غير المصرح بها. كل نسخة مرتبطة بمعرف ترخيص فريد (`UNIQUE_LICENSE_ID`) وبصمة جهاز محددة.

## 🎯 آلية عمل نظام الحماية

### 1. **معرف الترخيص الفريد (Unique License ID)**
- كل نسخة من التطبيق تحتوي على معرف فريد
- يتم تضمين المعرف في الكود المشفر
- لا يمكن تغيير المعرف بعد بناء التطبيق (APK)

### 2. **بصمة الجهاز (Device Fingerprint)**
- عند أول تشغيل، يتم أخذ بصمة فريدة للجهاز
- تتضمن البصمة: معرف الجهاز، الموديل، الشركة المصنعة، رقم الهاردوير
- يتم تشفير البصمة وحفظها محلياً

### 3. **ربط الترخيص بالجهاز**
- عند كل تشغيل، يتم التحقق من مطابقة بصمة الجهاز الحالية مع المحفوظة
- إذا تم نقل التطبيق لجهاز آخر، سيتم رفض التشغيل
- رسالة خطأ واضحة تظهر للمستخدم

### 4. **التشفير والحماية**
- الكود يتم تشفيره عند البناء باستخدام `--obfuscate`
- ملفات الخرائط (maps) للتشفير يتم حفظها بشكل آمن
- لا يمكن عكس هندسة الكود بسهولة

## 🚀 كيفية إنشاء نسخة جديدة لعميل

### الخطوة 1: تغيير معرف الترخيص

افتح الملف: `lib/core/config/app_license.dart`

```dart
static const String UNIQUE_LICENSE_ID = 'MAHER_CLIENT_XXX_[RANDOM_CODE]';
```

**أمثلة على معرفات فريدة:**
```dart
// العميل 1
'MAHER_CLIENT_001_XK9P2LMN4R7'

// العميل 2  
'MAHER_CLIENT_002_WQ5T8HJV3M1'

// العميل 3
'MAHER_CLIENT_003_ZC6Y1NDP9K4'

// العميل 4
'MAHER_CLIENT_004_PT3B7XFN2QW'
```

**💡 نصيحة:** استخدم مولد أحرف عشوائية أو موقع مثل:
- https://www.random.org/strings/
- اطلب 10 أحرف (A-Z, 0-9)

### الخطوة 2: تحديث معلومات العميل (اختياري)

```dart
static const String CLIENT_NAME = 'Client_XXX';  // اسم العميل للتتبع الداخلي
static const String ISSUE_DATE = '2025-01-19';   // تاريخ الإصدار
```

### الخطوة 3: بناء APK المشفر

#### 🔒 البناء مع التشفير الكامل (موصى به للإنتاج)

```bash
cd /home/user/Maherkh

# بناء APK مشفر للإصدار
flutter build apk --release \
  --obfuscate \
  --split-debug-info=build/debug-info/client_XXX

# أو بناء App Bundle مشفر
flutter build appbundle --release \
  --obfuscate \
  --split-debug-info=build/debug-info/client_XXX
```

**معاني الخيارات:**
- `--release`: بناء نسخة الإصدار النهائي
- `--obfuscate`: تشفير الكود وجعله غير قابل للقراءة
- `--split-debug-info`: حفظ ملفات فك التشفير (احتفظ بها في مكان آمن!)

#### ⚡ البناء السريع (للاختبار فقط)

```bash
cd /home/user/Maherkh
flutter build apk --release
```

### الخطوة 4: اختبار النسخة

```bash
# تثبيت على جهاز اختبار
adb install build/app/outputs/flutter-apk/app-release.apk

# أول تشغيل سيربط التطبيق بهذا الجهاز
# تأكد من ظهور رسالة "✅ التطبيق مرخص ومحمي"
```

### الخطوة 5: حفظ ملفات التشفير

```bash
# احفظ ملفات فك التشفير في مكان آمن (مهم جداً!)
cp -r build/debug-info/client_XXX ~/backups/maherkh_client_XXX_symbols/
```

**⚠️ تحذير مهم:** ملفات `debug-info` ضرورية لفهم تقارير الأخطاء. احتفظ بها في مكان آمن!

## 📊 سيناريوهات الاستخدام

### ✅ السيناريو الطبيعي (عميل شرعي)

1. العميل يتلقى ملف APK
2. يثبته على جهازه
3. عند أول تشغيل: يتم ربط التطبيق بجهازه
4. يعمل التطبيق بشكل طبيعي على نفس الجهاز

### ❌ محاولة النسخ (مشاركة غير مصرح بها)

1. شخص ينسخ ملف APK من جهاز العميل
2. يحاول تثبيته على جهاز آخر
3. عند التشغيل: يكتشف النظام اختلاف بصمة الجهاز
4. يظهر خطأ: "⚠️ هذا التطبيق مرخص لجهاز آخر"
5. التطبيق لا يعمل ويطلب التواصل مع المطور

### 🔄 إعادة التثبيت على نفس الجهاز

- إذا قام العميل بحذف التطبيق وإعادة تثبيته على **نفس الجهاز**
- سيعمل التطبيق بشكل طبيعي (البصمة محفوظة في SharedPreferences)

### 📱 تغيير الجهاز أو إعادة ضبط المصنع

- إذا قام العميل بتغيير جهازه أو عمل Reset للجهاز
- البصمة ستتغير والتطبيق لن يعمل
- سيحتاج لنسخة جديدة بمعرف ترخيص جديد

## 🛡️ مستويات الحماية المطبقة

### 🔒 المستوى 1: معرف الترخيص الفريد
- ✅ كل نسخة لها معرف مختلف
- ✅ المعرف مضمّن في الكود المشفر
- ✅ لا يمكن تعديله بعد البناء

### 🔒 المستوى 2: ربط الجهاز
- ✅ بصمة فريدة للجهاز (Android ID + معلومات الهاردوير)
- ✅ التحقق عند كل تشغيل
- ✅ منع التشغيل على أجهزة مختلفة

### 🔒 المستوى 3: تشفير الكود
- ✅ الكود مشفر بالكامل (`--obfuscate`)
- ✅ أسماء الدوال والمتغيرات مشفرة
- ✅ صعوبة عكس الهندسة

### 🔒 المستوى 4: حماية SharedPreferences
- ✅ البيانات محفوظة محلياً ومشفرة
- ✅ استخدام SHA-256 للبصمات
- ✅ لا يمكن التلاعب بها بسهولة

## 📝 ملفات النظام

```
lib/
├── core/
│   ├── config/
│   │   └── app_license.dart          # ⚠️ غيّر UNIQUE_LICENSE_ID هنا
│   └── security/
│       ├── device_fingerprint.dart    # نظام بصمة الجهاز
│       └── license_validator.dart     # نظام التحقق من الترخيص
└── main.dart                          # نقطة الدخول مع التحقق
```

## 🔧 أوامر مفيدة

### فحص وتحليل الكود
```bash
cd /home/user/Maherkh
flutter analyze
flutter pub get
```

### بناء لمنصات مختلفة
```bash
# Android APK مشفر
flutter build apk --release --obfuscate --split-debug-info=build/symbols

# Android App Bundle مشفر
flutter build appbundle --release --obfuscate --split-debug-info=build/symbols

# iOS (إذا كان متاح)
flutter build ipa --release --obfuscate --split-debug-info=build/symbols
```

### اختبار على الأجهزة
```bash
# عرض الأجهزة المتصلة
flutter devices

# تشغيل على جهاز محدد
flutter run --release -d [device_id]

# تثبيت APK
adb install build/app/outputs/flutter-apk/app-release.apk
```

## 🔍 معلومات الترخيص في التطبيق

المستخدم يمكنه رؤية معلومات الترخيص من خلال:
1. الضغط على أيقونة (ℹ️) في AppBar
2. سيظهر حوار يعرض:
   - معرف الترخيص
   - حالة التفعيل
   - تاريخ التفعيل
   - عدد مرات التشغيل
   - معلومات الجهاز

## 📞 الدعم الفني

### للمطور:
- تأكد من حفظ ملف `app_license.dart` مع معرف كل عميل
- احتفظ بنسخة من ملفات `debug-info` لكل نسخة
- سجّل معرف الترخيص مع اسم العميل في قاعدة بيانات

### للعميل:
- عند ظهور خطأ في الترخيص، تواصل مع المطور
- لا تحاول نسخ التطبيق لأجهزة أخرى
- النسخة مرخصة لجهاز واحد فقط

## ⚠️ ملاحظات مهمة

1. **لا تنشر ملف `app_license.dart` على GitHub** (إذا كان يحتوي على معلومات حساسة)
2. **احتفظ بنسخة من ملفات `split-debug-info`** لكل عميل
3. **غيّر `UNIQUE_LICENSE_ID` لكل عميل** قبل البناء
4. **اختبر النسخة على جهاز قبل التسليم** للتأكد من عمل الحماية
5. **وثّق معرف الترخيص مع اسم العميل** للرجوع إليه لاحقاً

## 🎯 قائمة التحقق قبل التسليم

- [ ] تم تغيير `UNIQUE_LICENSE_ID` في `app_license.dart`
- [ ] تم تحديث `CLIENT_NAME` و `ISSUE_DATE`
- [ ] تم بناء APK مع `--obfuscate`
- [ ] تم حفظ ملفات `split-debug-info`
- [ ] تم اختبار النسخة على جهاز
- [ ] تم التأكد من ظهور رسالة "✅ التطبيق مرخص ومحمي"
- [ ] تم توثيق معرف الترخيص مع بيانات العميل
- [ ] تم حفظ نسخة من الكود مع معرف الترخيص

---

## 📚 موارد إضافية

- [Flutter Obfuscation](https://docs.flutter.dev/deployment/obfuscate)
- [Device Info Plus Package](https://pub.dev/packages/device_info_plus)
- [Shared Preferences](https://pub.dev/packages/shared_preferences)
- [Crypto Package](https://pub.dev/packages/crypto)

---

**تم إنشاء هذا النظام بواسطة:** Gen AI Agent  
**التاريخ:** 2025-01-19  
**النسخة:** 1.0.0
